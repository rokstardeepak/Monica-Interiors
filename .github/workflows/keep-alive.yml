name: Keep Render App Alive
on:
  schedule:
    - cron: '*/10 * * * *' # Runs every 10 minutes
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping website
        run: curl -s https://monica-interiors.onrender.com > /dev/null
